interface:
  display_name: "Rate Limit This"
  short_description: "Design human-approved application rate limits"
  default_prompt: "Use $rate-limit-this to audit this app, draft RATE_LIMITS.md for my review, and implement it only after approval."
policy:
  allow_implicit_invocation: false
