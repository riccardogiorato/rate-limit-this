interface:
  display_name: "Rate Limit This"
  short_description: "Design human-approved application rate limits"
  default_prompt: "Use $rate-limit-this to audit this app and draft a root RATE_LIMITS.md, then stop for my review."
policy:
  allow_implicit_invocation: false
